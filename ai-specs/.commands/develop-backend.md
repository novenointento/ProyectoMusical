Por favor, analiza y resuelve el ticket de Jira: $ARGUMENTS.

Sigue estos pasos:

1. Entiende el problema descrito en el ticket.
2. Busca en el código los archivos relevantes.
3. Crea una nueva rama usando el ID del ticket (por ejemplo, `SCRUM-1`).
4. Implementa los cambios necesarios para resolver el ticket, siguiendo el orden de las tareas y asegurando que completas todo en secuencia (incluyendo escribir y ejecutar pruebas, actualizar documentación, etc.).
5. Asegura que el código pase linting y verificación de tipos.
6. Añade al commit solo los archivos afectados por el ticket, dejando fuera cualquier otro cambio no relacionado. Crea un mensaje de commit descriptivo.
7. Haz push y crea una PR usando el ID del ticket (por ejemplo, `SCRUM-1`) para que quede vinculada en Jira.

Recuerda usar GitHub CLI (`gh`) para todas las tareas relacionadas con GitHub.
